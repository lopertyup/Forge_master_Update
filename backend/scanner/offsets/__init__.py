"""
============================================================
  FORGE MASTER — Capture offsets

  Per-side sub-zone ratios used by the scanner. Two modules:

    opponent  — full enemy profile screen (8 equipment cells +
                pets + mount + skills strip).
    player    — player's own equipment panel (8 cells, no
                companions / skills).
============================================================
"""
