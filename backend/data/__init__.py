"""
============================================================
  FORGE MASTER — Game data subpackage

  Lazy loaders for the JSON resources stored under
  ``<project_root>/data/``. Anything that needs to read those
  JSON files goes through ``data.libraries`` so the cache is
  shared.
============================================================
"""
