"""
============================================================
  FORGE MASTER — Combat simulation subpackage

  ``engine`` holds the tick-by-tick PvP combat simulator
  (Fighter, SkillInstance, simulate, simulate_batch). It is
  the single source of truth for "who would win" decisions.
============================================================
"""
